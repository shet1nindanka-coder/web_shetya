# Открытые вопросы (выжимка из аудитов)

Сжато 17.08.2026 из трёх отчётов аудита 03.08, плана исправлений и аудита готовности к пилоту
10.08 (коммит `a0bdc89`). Полные отчёты удалены.

**Уже сделано (проверено по коду 17.08):** волны 0–1; волна 5 — скоуп по владельцу
(`ownStudentsWhere`/`ownResourceWhere` в `lib/platform-data.ts`, скоуп файлов, миграция
`20260805090000_student_teacher_owner`); все четыре блокера отчёта 10.08 — Б-1 сброс пароля
(`resetStudentPasswordAction`), Б-2 импорт не пишет `answerLatex: null` поверх ручного
(`buildImportUpdateData` + тесты), Б-3 guard в `prisma/seed.ts` (только localhost, обход
`ALLOW_DESTRUCTIVE_SEED=1`) + `db:reset:local`, Б-4 смена владельца ученика у DEVELOPER
(`changeStudentOwnersAction`). **Главный оставшийся блокер пилота — SEC-001 (fake GREEN), волна 2.**

**Решения владельца (не переоткрывать):** доступ ученика к любому номеру — задуманное поведение
(SEC-004 снят); но 11.08 эталонные ответы ученикам убраны совсем (`answerLatex` занулён в
студенческих читалках, спойлер убран) — не возвращать; согласие родителей не требуется (SEC-008
снят); отмена ДЗ удаляет всё по-настоящему; повторная выдача блокируется только для решённых
(GREEN/YELLOW); вместо статистики учителя (PROD-007) — экспорт статистики группы.

---

## Предстартовый чек-лист (до первого живого ученика, порядок важен)

**1. Бэкап прод-БД.** Статус неизвестен. Всё, что ниже, необратимо.

**2. Проверить прод-БД одним заходом** (`psql "$DATABASE_URL"`):

```sql
-- Миграции: применены все, ни одной failed (P3009)
SELECT migration_name, finished_at, rolled_back_at FROM "_prisma_migrations" ORDER BY started_at;

-- SEC-009: живы ли сид-аккаунты со слабыми паролями
SELECT id, email, role FROM "User"
WHERE lower(email) IN ('dev@example.com','teacher@example.com','ilya@example.com','maria@example.com');

-- Владельцы учеников (блокер Б-4)
SELECT role, count(*) FROM "User" GROUP BY role;
SELECT id, email, "createdAt" FROM "User" WHERE role='TEACHER' ORDER BY "createdAt";
SELECT id, email, "teacherId" FROM "User" WHERE role='STUDENT' AND "teacherId" IS NULL;

-- Форма таблицы Notification (М-11): создана ли db push'ем с другими колонками
SELECT column_name, data_type, is_nullable FROM information_schema.columns
WHERE table_name='Notification' ORDER BY ordinal_position;

-- Номера с заполненным ответом и пустым условием — прямая цена блокера Б-2
SELECT count(*) FROM "TopicHomeworkNumber"
WHERE "answerLatex" IS NOT NULL AND btrim("answerLatex") <> ''
  AND ("conditionLatex" IS NULL OR btrim("conditionLatex") = '');

-- Близнецы номеров по ведущим нулям
SELECT "topicId", ltrim("number",'0') AS norm, count(*), array_agg("number")
FROM "TopicHomeworkNumber" GROUP BY 1,2 HAVING count(*) > 1;
```

**3. Задеплоить волну 5** (`20260805090000_student_teacher_owner`) **до** создания второго
учителя, затем повторить запрос про `teacherId IS NULL`.

**4. Проверить окружение на VPS:**

```bash
pm2 env $(pm2 id shetya | tr -d '[]') | grep -E '^TZ='   # должно быть Europe/Moscow
grep client_max_body_size /etc/nginx/sites-enabled/*      # должно быть >= 16m
ls -l .env && stat -c '%a' .env                           # права 600
```

**5. Аудитор не смог проверить (проверить руками):** состояние прод-БД (SQL выше); задеплоена ли
уже волна 5; nginx (`client_max_body_size`, `proxy_read_timeout`, `X-Real-IP`) и `TZ` в pm2;
форму ошибок ProxyAPI (влияет на Р-12/Р-28); утечку счётчика SSE-соединений при жёстком обрыве
(после двух обрывов ученик может ловить постоянный 429 до `pm2 restart`); HEIC→JPEG в iOS Safari;
наличие и свежесть бэкапа.

---

## Блокеры отчёта 10.08 — ✅ все закрыты в коде

Остаётся только операционная часть: **волна 5 должна быть задеплоена на прод ДО создания второго
учителя** — бэкфилл миграции `20260805090000` привязывает всех учеников к самому раннему учителю,
если второй учитель заведён до деплоя, его ученики перепишутся на №1 (лечится теперь через смену
владельца на вкладке аккаунтов DEVELOPER). Если учителей в БД нет (восстановление из бэкапа) —
все получат `teacherId = NULL` («без учителя» подсвечивается там же).

---

## Волны плана исправлений 03.08 (осталось: 2, 3, 4, 6, 7, 8)

**Волна 2 — целостность автопроверки. ГЛАВНЫЙ ОТКРЫТЫЙ БЛОКЕР ПИЛОТА.**
- SEC-001 (HIGH): ученик ставит себе GREEN надписью на фото — `injectionSuspected` сохраняется, но
  `applyVerdictsToProgress` его не смотрит. Три серверных слоя: (1) `injectionSuspected` → статус не
  пишется, «нужна проверка учителем»; (2) автоматический GREEN только при серверной сверке
  `recognizedAnswer` с `answerLatex` (нормализация), не совпало — жёлтый/учителю; (3) `confidence == 1.0`
  вместе с маркерами инструкций понижает доверие. Тесты: набор инъекционных ответов, ни один не даёт GREEN.
- SEC-005: эталонный ответ утекает через вывод проверки — редактировать `recognizedAnswer`/`comment`,
  совпадающие с `answerLatex`, для ученика.
- SEC-006 + PROD-010: глобальный дневной бюджет списывать вплотную к `enqueue` после подтверждения
  реального ДЗ; при FAILED — возврат слота. Почасовой лимит на ученика — до валидации.
- PROD-011: при низкой уверенности комментарий модели не показывать как утверждение.

**Волна 3 — надёжность в учебное время.** PROD-004: при старте процесса помечать `CHECKING`,
начатые до текущего запуска, как FAILED («проверка прервалась при обновлении сайта — запусти ещё
раз»), повтор без списания бюджета. ARCH-012: то же для `resumeStaleLessonPlans`. PROD-005+ARCH-039:
`app/global-error.tsx` по-русски, логировать `error.digest`; ARCH-009: `removeStoredFile` глушит
ошибки. PROD-020: убрать «Проверьте подключение к PostgreSQL» из UI учителя.

**Волна 4 — ученический путь.** PROD-006: клиентское сжатие фото (canvas ~2000px, JPEG q≈0.8),
загрузка по одному, понятный 413. PROD-009: русские сообщения вместо текста провайдера. PROD-019/ARCH-035:
единый `unauthorized()` с русским `{error}` в ~30 роутах. PROD-028: дедуп фото по хэшу. PROD-015:
заметка ученика — ошибка/ретрай/предупреждение о несохранённом.

**Волна 5 — скоуп по владельцу — ✅ сделана 05.08** (SEC-002/003 закрыты: viewer во всех читалках,
скоуп записей и файлов). Операционный остаток: задеплоить до второго учителя (см. выше).

**Волна 6 — экспорт статистики группы** (вместо PROD-007). Вынести сборку периода из
student/export/pdf-роута в `lib/report-period-data.ts`; роут `groups/[groupId]/export/pdf` с теми же
лимитами; макет `lib/group-report-pdf.tsx` (сводка → таблица «ученик × итог» → темы по доле красных →
разворот по ученику); кнопка на странице группы. Заодно PROD-023 («занятий за неделю не было» в
годовом отчёте) и PROD-013 (даты без года).

**Волна 7 — гигиена безопасности.** SEC-007: имя/email детей — не в текст логов, IP только в
счётчиках. SEC-009+SEC-025: сид-аккаунты на проде, слабые пароли, проверка силы пароля в
`create-developer.ts`. SEC-015…SEC-031: `Secure` безусловно в проде, проверка Origin, HSTS с
`includeSubDomains`, cap на массовую рассылку.

**Волна 8 — учительская полировка.** PROD-012 (дедлайн в прошлом — тж. Р-23) · PROD-021 (автосейв
помечает несохранённое сохранённым) · PROD-022 (формы теряют введённое при ошибке — тж. Р-14/Р-21) ·
PROD-024 (ложная метка «Уже в ДЗ») · PROD-026 · PROD-014 (ИИ молча собирает урок на 12 мин вместо 60) ·
ARCH-019 (статус ДЗ считается 4 способами) · ARCH-038 (клиентское «сегодня» по TZ браузера) ·
ARCH-003 (мёртвый `createTopicAction`) · ARCH-028 (зависший `issuedAssignmentId`) · ARCH-036
(дев-панель не инвалидирует кэш).

**Отложено после пилота:** ARCH-004 (globals.css: два поколения токенов, 44% мёртвых классов),
ARCH-005 (распил `platform-data.ts`), ARCH-014 (тройной copy-paste board-компонентов), ARCH-034
(два дизайн-языка), ARCH-020 (полная матрица «тема × номер × ученик» в памяти — рванёт в конце
четверти), ARCH-022 (тесты не покрывают БД/API/права), архивирование вместо удаления.

---

## Риски из аудита 10.08 (пилот не срывают; Р-1…Р-5 — в первую неделю)

- Р-1. Стрик привязан к моменту ответа модели, а не работы ученика: сдача около полуночи рвёт серию.
- Р-2. Учитель, разбирающий ДЗ пачкой, задним числом стирает дни активности ученика.
- Р-3. Общий IP-лимит съедается успешными входами: класс за одним Wi-Fi блокирует себя на первом уроке.
- Р-4. Ученик не может отправить обещанные 10 фото: 15 МБ на всю пачку, всё или ничего.
- Р-5. SSE-канал раздаёт события обо всех учениках платформы каждому учителю.
- Р-6. Разработчик может положить чужого ученика в группу учителя — тот получит полный доступ.
- Р-7. Диалог удаления ученика не называет главного: удаляются все ДЗ, фото и история проверок.
- Р-8. ARCH-032: проверка остаётся DONE с непринятыми статусами навсегда.
- Р-9. Повторный клик по активной кнопке итога урока обнуляет статус номера.
- Р-10. «Пересобрать для этого ученика» стирает проставленные итоги занятия без подтверждения.
- Р-11. Отменённое ДЗ навсегда ломает черновик ИИ-ДЗ: UI предлагает «Выдать», сервер молча отказывает.
- Р-12. Проверка, честно стоящая в очереди, через 15 минут объявляется «прервавшейся из-за рестарта».
- Р-13. Ручные правки набора во время работы ИИ-подбора молча затираются.
- Р-14. Ошибка на странице темы стирает всю правку, включая длинный список номеров.
- Р-15. После импорта поле «Номера» остаётся устаревшим — сохранение формы предлагает удалить импортированное.
- Р-16. Решённый номер невозможно перевыдать после отмены ДЗ: ошибка ведёт на несуществующую карточку.
- Р-17. Отмена ДЗ: ошибка после транзакции даёт 500 «Не удалось отменить», хотя ДЗ уже удалено.
- Р-18. Имя, логин и IP ученика пишутся в логи открытым текстом (SEC-007).
- Р-19. Забывший пароль ученик блокирует себе вход на 15 минут со всех устройств, разблокировать некому.
- Р-20. Пароль ученика виден один раз и нигде не повторяется.
- Р-21. Форма создания ученика/аккаунта полностью очищается при ошибке сервера.
- Р-22. Замена файлов темы через server action с лимитом 15 МБ → красный экран и потеря правки.
- Р-23. ДЗ можно выдать с дедлайном в прошлом — сразу «Просрочено».
- Р-24. Отчёт «за учебный год» подписан «01.09.2025 — 10.08.2026», а график строит только 112 дней.
- Р-25. Индикатор бюджета ИИ в дев-панели показывает не то число, которое блокирует учеников.
- Р-26. Исчерпанный лимит выглядит как «подождите немного», хотя ждать до конца часа или до 03:00 МСК.
- Р-27. Опечатка в поле «Модель» дев-панели выключает автопроверку для всех и жжёт бюджет.
- Р-28. Проверка считается успешной, даже если модель прислала вердикты не по всем номерам.
- Р-29. `TZ` приходит только из `.env`, стартовой проверки нет — потеря переменной молча сдвигает «сегодня».
- Р-30. `check:schema` не сверяет типы, nullability, enum, индексы и `@@unique` — ложная уверенность.
- Р-31. PDF-кнопки — обычные ссылки: при неудаче учитель попадает на голую страницу с английским текстом.

## Мелочи (чинить по мере касания кода)

- М-1. `countStoredFileUsages` глушит любые ошибки (`.catch(() => 0)`) — файл могут удалить вместе с
  фото решения; сузить до P2021.
- М-2. Удаление темы оставляет осиротевшие `StoredFile` с фото учеников (S3-объекты навсегда).
- М-3. Автоудаление фото смотрит только на возраст: снижение `photoRetentionDays` в панели снимет
  фото с активных ДЗ; исключать ДЗ с будущим дедлайном/без завершённой проверки.
- М-4. `parseNumbersInput` молча обрезает список на 2000 номерах.
- М-5. `confidence` строкой («0.9») обнуляет автопроверку молча; принимать числовую строку.
- М-6. `AI_CHECK_BASE_URL` по умолчанию — OpenRouter: потеря переменной уводит ключ и фото другому
  провайдеру; сделать переменную обязательной.
- М-7. Дневное окно бюджета обнуляется в 03:00 МСК (UTC-выравнивание).
- М-8. Дедлайн в списках без времени, «Просрочено» — по времени; заменить на `formatDateTime`.
- М-9. Форматтеры дат без `timeZone: "Europe/Moscow"` — расхождение рендера и гидратации не из МСК.
- М-10. Имена PDF-файлов датируются по UTC (`toISOString().slice(0,10)`).
- М-11. Миграция `Notification` — `CREATE TABLE IF NOT EXISTS`: не гарантирует форму таблицы после
  исторического `db push`; `lib/notifications.ts` глушит ошибку — колокольчик молча пуст.
- М-12. Генерация плана, начатая <2 мин до рестарта, не возобновляется (`RESUME_MIN_AGE_MS`).
- М-13. Ошибка формата фото называет PDF/DOCX, которые сдавать нельзя; проверять
  `allowedPhotoExtensions` первым.
- М-14. Загрузку фото нельзя отменить, таймаута нет (голый `fetch` без `AbortController`).
- М-15. Логин не возвращается в поле после ошибки; тёзки в группе неразличимы (показывать
  `имя (email)`); not-found ведёт учителя на `/student`.
