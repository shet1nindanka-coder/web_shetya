-- Номер задачи становится текстом: нумерация задачников шестизначная и с ведущими
-- нулями («010203»), которые Int терял ещё при записи. Существующие значения
-- сохраняются как есть: «1» остаётся «1», а не «000001».
ALTER TABLE "TopicHomeworkNumber"
  ALTER COLUMN "number" TYPE TEXT USING "number"::text;
