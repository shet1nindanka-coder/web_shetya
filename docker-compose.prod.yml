services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tutorflow-app:latest
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "3000:3000"
